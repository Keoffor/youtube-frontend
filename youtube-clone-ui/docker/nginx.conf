server {
    listen 80;
    server_name utube.kendoc.vip;



    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;

    }
    client_max_body_size 100M;

}
